# VM Builder Web

Standalone React + Vite frontend for the VM Builder MVP.

## Scripts

- `npm install`
- `npm run dev`
- `npm run build`

## Modes

- Default: mock client with local session state.
- HTTP mode: set `VITE_API_MODE=http` and `VITE_API_URL=/api` to talk to FastAPI.
